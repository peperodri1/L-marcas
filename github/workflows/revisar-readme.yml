name: Crear o modificar README.md

on:
  push:
    branches: [main]

jobs:
  actualizar-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar el repositorio
        uses: actions/checkout@v3

      - name: Escribir contenido en README.md
        run: |
          echo "## Bienvenido a mi proyecto 🚀" > README.md
          echo "" >> README.md
          echo "Este archivo ha sido actualizado automáticamente por un workflow de GitHub Actions." >> README.md

      - name: Mostrar contenido del archivo
        run: cat README.md
